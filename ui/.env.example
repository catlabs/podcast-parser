# Vite picks up VITE_* vars at build time (and dev-server start time).
# Copy this file to ui/.env.local to override defaults without committing them.

# Backend API base URL.
# Default if unset: http://localhost:8000
VITE_API_BASE_URL=http://localhost:8000
