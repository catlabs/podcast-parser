# Claude Code — project rules

## Git

- **Never commit unless the user explicitly asks.** Do not commit after edits, refactors, or any other changes — wait for a clear instruction like "commit" or "commit the changes".
