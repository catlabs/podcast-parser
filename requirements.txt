# Backend runtime dependencies.
# Versions are intentionally loose — pin in CI once an Azure target is chosen.

# ── Web layer ────────────────────────────────────────────────────────────────
fastapi
uvicorn
pydantic
python-dotenv

# ── Ingestion ────────────────────────────────────────────────────────────────
feedparser
requests
openai-whisper
yt-dlp

# ── Indexing / retrieval ─────────────────────────────────────────────────────
sentence-transformers
chromadb

# ── LLM providers ────────────────────────────────────────────────────────────
anthropic
openai

# ── Research orchestration ───────────────────────────────────────────────────
langgraph

# ── CLI front door (Phase 1.1e) ──────────────────────────────────────────────
# Typer drives the ``rag.cli`` entry points; rich handles streamed token
# rendering and per-agent progress indicators in the terminal.
typer>=0.12
rich>=13.7

# ── Observability ────────────────────────────────────────────────────────────
# Opt-in: tracing is no-op unless LANGFUSE_PUBLIC_KEY + LANGFUSE_SECRET_KEY
# are set in .env. See MIGRATION.md Step 10 and .ai/README.md for details.
langfuse
# Pure-OTel side track (warm-up step A): one call site instrumented with
# canonical gen_ai.* attributes, shipped via OTLP HTTP to Langfuse's OTel
# endpoint. Activated by OTEL_ENABLED=true; otherwise the bootstrap no-ops.
opentelemetry-sdk
opentelemetry-exporter-otlp-proto-http

# ── Storage providers ────────────────────────────────────────────────────────
# Opt-in: AzureBlobObjectStore loads only when AZURE_STORAGE_ACCOUNT +
# AZURE_STORAGE_CONTAINER are set. Auth uses DefaultAzureCredential
# (Managed Identity → az login → env baseline). No connection strings,
# no account keys in any env file.
azure-storage-blob
azure-identity

# ── MCP (Phase 1.MCP) ────────────────────────────────────────────────────────
# Exposes ``rag.mcp_server`` over stdio for Claude Desktop. Major pinned —
# the MCP Python SDK is young and may break shape across majors.
mcp>=1.0,<2.0
